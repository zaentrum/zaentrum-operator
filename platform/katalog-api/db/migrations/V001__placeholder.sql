-- V001 placeholder. Real migrations will mirror the read surface of the CDS
-- model in katalog-manager-api/db once schema is settled. katalog-api uses
-- the read-only role `cloud_katalog_ro` and never owns DDL.
SELECT 1;
