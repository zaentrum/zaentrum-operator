image: registry.example.org/example/worker:1.0.0
replicas: 1
greeting: hello
# auth.token is a secret input (writeOnly); auth.signingKey is generated by the
# operator (x-zaentrum-generate). Neither has a default.
auth: {}
