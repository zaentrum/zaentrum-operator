apiVersion: v1
kind: Secret
metadata:
  name: worker
  labels:
    app.kubernetes.io/name: worker
type: Opaque
data:
  token: {{ .Values.auth.token | b64enc | quote }}
  signingKey: {{ .Values.auth.signingKey | b64enc | quote }}
