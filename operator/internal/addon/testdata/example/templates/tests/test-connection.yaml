apiVersion: v1
kind: Pod
metadata:
  name: worker-test-connection
  annotations:
    helm.sh/hook: test
spec:
  restartPolicy: Never
  containers:
    - name: probe
      image: registry.example.org/example/probe:1.0.0
      args: ["http://worker/.well-known/zaentrum-capability.json"]
